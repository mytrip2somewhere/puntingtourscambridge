# dependencies
node_modules/

# build output (regenerated by `npm run build`; Cloudflare Pages builds it in CI)
dist/

# os / editor
.DS_Store
*.log

# secrets (never commit)
.env
.env.*

# Claude Code local settings (may contain tokens)
.claude/settings.local.json
_incoming/
/images/
*.MOV
*.mov
