# site.config.md — Per-Site Configuration (BLANK TEMPLATE)

This is the ONLY file you fill in to launch a new site. Duplicate this file into the
new site's folder and complete every field. The pipeline (CLAUDE.md + `.claude/`
phase files) reads from here and otherwise stays generic. Do not reuse another
site's filled config; start from this blank each time so no old values leak through.

---

## IDENTITY (ask Oleg)
- Site name:
- Domain:
- Exact-match domain (EMD)? (yes/no):
- Primary keyword (the head term the domain owns):
- Location / destination focus:

## MODEL
- Affiliate links to GetYourGuide / Viator to a real operator Oleg sends traffic for and help with SEO.
- Primary KPI: affiliate clicks / bookings

## AUTHOR (must be a real person)
- Name:
- Role (honest, e.g. travel writer/researcher who has done this tour):
- Real credentials / firsthand experience (what they actually did):
- Photo path:
- Verification line basis (what facts they verify, against which official sources):

## TOURS
Paste the GYG / Viator affiliate links (with affiliate parameters) below at intake.
These are the definitive set. The pipeline pulls name, price, rating, and booking
volume from each, confirms the list back to Oleg, and sorts by booking volume. Stats
are attributed to their real source (operator / GYG / Viator), never claimed as the
site's own.
-
-
-

## COMPLIANCE MODULE
- Status: (ON / OFF)
- Items to research (verified, each with source + "last verified" date):
  -
  -
- (If OFF, leave items empty. If ON, list every site-specific item, e.g. licensing,
  age limits, local regulations, safety rules, booking requirements.)

## REAL-USER-LANGUAGE SOURCES (for Phase 1.2 forum mining)
- Subreddits:
- Forums:
- (replace per destination)

## SOCIAL LINKS (footer icons: Instagram, TikTok, Facebook, YouTube)
For each platform, use the real account URL if you have one. If not, use the
platform-search fallback pointing at the brand name (like the krabiboat.tours
pattern), which can be swapped for a real account later by editing this file.
- Instagram:
- TikTok:
- Facebook:
- YouTube:

## FTC DISCLOSURE
- Placement: footer, sitewide
- Disclosure text:

## ANALYTICS
- Google Analytics 4 measurement ID (unique per site, e.g. G-XXXXXXXXXX):

## VERIFICATION (search-engine ownership; placed in base layout <head>, same on every page)
- Google Search Console (google-site-verification content):
- Bing Webmaster Tools (msvalidate.01 content):

## SITE-SPECIFIC NOTES
-
